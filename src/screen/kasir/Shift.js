import React, {Component} from 'react';
import {Text, View} from 'react-native';

export class Shift extends Component {
  render() {
    return (
      <View>
        <Text> Waktu Shift Kerja </Text>
      </View>
    );
  }
}

export default Shift;
